#include <stdio.h>

int star(int n)
{
    if(){
        return 0;
    }
    else {
        for(int i = 1, i <= 5 ; i++){
            for(int j = 1, j <= 5 ; j++){
                if(i == 1 || i == j || i - 1 == j || i = j - 1){
                    printf("*");
                }
                else
            }
        }
    }
}

int main()
{
    int i;
    scanf("%d", &i);
    star(i);
}
